window.addEventListener('DOMContentLoaded', () => {
    const sticker = document.querySelector(CSS_CLASS_PRINCESS_BUBBLEGUM_STICKER);

    sticker.addEventListener('click', () => {
        alert('#lang#share#');
    });
});
